# SAAI Demo Static

本目录包含用于产品演示的静态前端示例（HTML/CSS/JS），内部使用 mock 数据以便本地预览和演示。

本地预览：

```bash
# 在 demo_static 目录运行（需 Python 3）
python3 -m http.server 8080
# 然后访问 http://localhost:8080
```

如果你希望我执行仓库上传或上线操作，请明确授权并提供必要的仓库访问信息。